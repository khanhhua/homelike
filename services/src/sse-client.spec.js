import { query, registerClient, sendTo } from './sse-client';

describe('sse-client', () => {
  describe('query', () => {
    it('should pass', () => {

    });
  });

  describe('registerClient', () => {
    it('should pass', () => {

    });
  });

  describe('sendTo', () => {
    it('should pass', () => {

    });
  });
});
